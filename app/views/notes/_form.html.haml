= simple_form_for @note do |f|
  %p How's your day going?
  = f.select :emoji, ['💩', '😊','😡','😏','🙃']
  %p Write a sentence or two why
  = f.input :content, label: false
  = f.button :submit, 'Send!'
